version: '3.8'

services:
  api:
    container_name: api
    build:
      context: .
      dockerfile: Dockerfile.api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./duckchat:/app/duckchat
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/providers"]
      interval: 30s
      timeout: 10s
      retries: 3

  react-app:
    container_name: react-app
    build:
      context: ./react-app
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    env_file:
      - .env
    depends_on:
      - api

  ollama:
    image: ollama/ollama
    container_name: ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_models:/root/.ollama
    restart: unless-stopped

volumes:
  ollama_models:
